# Demo TPC-H App

This is a dummy repository used for testing the POV3 PR Agent's codebase graph integration.
It contains raw SQL files querying the `SNOWFLAKE_SAMPLE_DATA.TPCH_SF1` schema in `queries/` and Python code with embedded SQL in `src/`.
